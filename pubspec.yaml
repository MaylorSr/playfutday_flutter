name: playfutday_flutter
description: Project Flutter for the screen like a red social of a people that likes football.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0-109.0.dev <4.0.0"

dependencies:
  flutter_form_bloc: ^0.30.1
  bloc: ^8.1.1
  flutter_bloc: ^8.1.1
  equatable: ^2.0.0
  http: ^0.13.5
  get_it: ^7.2.0
  injectable: ^2.1.0
  http_interceptor: ^1.0.1
  shared_preferences: ^2.0.17
  cupertino_icons: ^1.0.2
  get:
    ^4.6.1
    #image_picker: ^0.6.7+12
  image_picker: ^0.8.6+1
  #image_cropper: ^1.3.1
  image_cropper: ^3.0.1
  curved_navigation_bar: ^1.0.3 #latest version

  flutter:
    sdk: flutter
  bloc_concurrency: ^0.2.1
  stream_transform: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
